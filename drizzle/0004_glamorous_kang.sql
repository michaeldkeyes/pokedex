CREATE TABLE `abilities` (
	`ability` text PRIMARY KEY NOT NULL
);
--> statement-breakpoint
ALTER TABLE `pokemon` RENAME COLUMN `abilities` TO `ability_one`;--> statement-breakpoint
/*
 SQLite does not support "Set not null to column" out of the box, we do not generate automatic migration for that, so it has to be done manually
 Please refer to: https://www.techonthenet.com/sqlite/tables/alter_table.php
                  https://www.sqlite.org/lang_altertable.html
                  https://stackoverflow.com/questions/2083543/modify-a-columns-type-in-sqlite3

 Due to that we don't generate migration automatically and it has to be done manually
*/--> statement-breakpoint
ALTER TABLE pokemon ADD `ability_two` text NOT NULL REFERENCES abilities(ability);--> statement-breakpoint
ALTER TABLE pokemon ADD `ability_three` text REFERENCES abilities(ability);--> statement-breakpoint
CREATE UNIQUE INDEX `abilities_ability_unique` ON `abilities` (`ability`);--> statement-breakpoint
/*
 SQLite does not support "Creating foreign key on existing column" out of the box, we do not generate automatic migration for that, so it has to be done manually
 Please refer to: https://www.techonthenet.com/sqlite/tables/alter_table.php
                  https://www.sqlite.org/lang_altertable.html

 Due to that we don't generate migration automatically and it has to be done manually
*/